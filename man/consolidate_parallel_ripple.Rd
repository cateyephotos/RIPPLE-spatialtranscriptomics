% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pipeline.R
\name{consolidate_parallel_ripple}
\alias{consolidate_parallel_ripple}
\title{Consolidate output from a parallel per-cell-type \code{run_ripple()} fan-out}
\usage{
consolidate_parallel_ripple(
  worker_dirs,
  output_dir,
  query_celltype,
  celltype_column = "cell_type",
  verbose = TRUE
)
}
\arguments{
\item{worker_dirs}{Character vector of per-worker RIPPLE output
directories. Each should be the exact directory each \code{run_ripple()}
call wrote to (typically \code{file.path(out_root, ct, analysis_name)}
from the vignette pattern).}

\item{output_dir}{Path for the consolidated results tree. Created if
missing.}

\item{query_celltype}{Query cell type label used in the parallel run.
Needed to correctly dedupe query rows in \code{cell_distances.csv.gz}.}

\item{celltype_column}{Name of the cell type column in the QC files
(default \code{"cell_type"}). Change only if the workers used a
nonstandard column name.}

\item{verbose}{Print progress (default \code{TRUE}).}
}
\value{
Invisibly, the \code{output_dir} path. The directory can be passed
directly as \code{results_dir} to \verb{\link{ripple_plot_qc}()} and the
other functions that expect a full RIPPLE results tree.
}
\description{
The \code{parallelization} vignette runs \code{run_ripple()} once per target cell
type inside \code{future_lapply()}, giving each worker its own \code{output_dir}.
That fragments the outputs: each worker's \verb{per_celltype/}, \verb{summary/},
and \verb{qc/} directories live under its own root. \code{ripple_plot_qc()} and
\code{merge_ripple_results()} both read from a single \code{results_dir} and
cannot see across worker roots. This helper merges the per-worker
outputs into a canonical results directory.
}
\details{
What it does:
\itemize{
\item Copies each worker's \verb{per_celltype/<ct>/} into
\verb{output_dir/per_celltype/}.
\item Concatenates \code{qc/cell_distances.csv.gz} across workers. Query
cells appear in every worker's file (each worker analyses query +
one target); the merge takes query rows from a single worker plus
target rows from every worker, so query counts are not inflated
N-fold in the composition and distance QC panels.
\item Calls \verb{\link{merge_ripple_results}()} on the consolidated tree
to rebuild \code{summary/all_genes_results.csv} and friends.
}

\code{sample_summary.csv} and \code{filtered_celltype_counts.csv} are not
merged: they are computed per-worker over the query + one target
subset and are not consumed by downstream RIPPLE functions. Anything
\verb{\link{ripple_plot_qc}()} needs is derived from
\code{cell_distances.csv.gz} and the merged summary.
}
\examples{
\dontrun{
# After the future_lapply() fan-out in the parallelization vignette:
worker_dirs <- file.path("ripple_out_parallel", targets, "ripple")

final_dir <- consolidate_parallel_ripple(
  worker_dirs    = worker_dirs,
  output_dir     = "ripple_out_parallel/combined",
  query_celltype = "endothelial"
)

ripple_plot_qc(final_dir, query_label = "endothelial")
}
}
\seealso{
\code{\link{merge_ripple_results}}, \code{\link{ripple_plot_qc}}. The
\code{parallelization} vignette (\code{vignette("parallelization")}) shows
the full fan-out and consolidation pattern.
}
